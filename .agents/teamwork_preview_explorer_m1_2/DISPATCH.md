## 2026-09-01T05:56:12Z
Task:
Investigate the exact changes needed in `packages/web/src/components/admin/AdminAIConfig.tsx` for verbatim upstream error diagnostics, multi-category classification, and the playground advisory banner:
1. Review `handleTestDraft` (around lines 315–480) and existing error evaluation (lines 460–474) and banner rendering (lines 746–759).
2. Design a robust error diagnostic parser function/logic:
   - Differentiate:
     a) `daily_cap`: 429 status where error text mentions daily limit, free tier limit, or 50 requests.
     b) `rate_limit`: 429 status for per-minute/concurrency burst limits (20 req/min).
     c) `congestion`: 503 Service Unavailable, 529 Site Overloaded, or error text mentioning model busy/high traffic/queue.
     d) `credits_exhausted`: 402 Payment Required or error text mentioning insufficient credits/balance.
     e) `auth_error`: 401 Unauthorized or invalid API key.
     f) `general`: any other upstream failure.
3. Formulate how `handleTestDraft` extracts the verbatim upstream error (`data?.error?.message || response.statusText || 'Unknown error'`).
4. Design the updated advisory banner in JSX:
   - Header with dynamic icon, color (amber/rose/purple), and clear category title.
   - Code block displaying the **Verbatim Upstream OpenRouter Error Message**.
   - Contextual Actionable Instructions (e.g. how to add credits, wait for rate limit reset, or switch models).
   - Clear visual presentation of the high-fidelity offline fallback draft generated by `generateSmartSupportReply(testThread)` with a `[⚡ Grounded Offline Synthesizer Fallback Active]` badge.
5. Identify all edge cases and ensure zero regression with the rest of the playground.
